# Sum of natural number 

def sum_num(n):
    # test for base case
    if n == 0:
        return 0 
    # make a calulation and return the result
    else:
        return(sum_num(n-1) + n)
    
print(sum_num(5))